{
  "name": "@smithy-mcp/cdk",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "synth": "cdk synth",
    "cdk": "cdk",
    "deploy:staging": "cdk deploy --all --context environment=staging --require-approval never",
    "deploy:production": "cdk deploy --all --context environment=production --require-approval never",
    "destroy": "cdk destroy --all"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.244.0",
    "constructs": "^10.6.0"
  },
  "devDependencies": {
    "@types/node": "^20.19.0",
    "aws-cdk": "^2.244.0",
    "esbuild": "^0.27.4",
    "typescript": "^5.8.0"
  }
}
